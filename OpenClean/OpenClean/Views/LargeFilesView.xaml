<UserControl x:Class="OpenClean.Views.LargeFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:OpenClean.Services.Localization"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FontFamily="{DynamicResource AppFont}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ===== Steuerzeile: Laufwerk, Mindestgröße, Suchen/Abbrechen ===== -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Padding="22,18" Margin="0,18,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{loc:Loc storage.selectDrive}" Style="{StaticResource MutedText}"
                               VerticalAlignment="Center" Margin="0,0,10,0" />
                    <ComboBox ItemsSource="{Binding Drives}" SelectedItem="{Binding SelectedDrive}"
                              MinWidth="160" VerticalAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TitleDisplay}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Text="{loc:Loc largefiles.minSize}" Style="{StaticResource MutedText}"
                               VerticalAlignment="Center" Margin="22,0,10,0" />
                    <ComboBox ItemsSource="{Binding MinSizeOptions}" SelectedItem="{Binding MinSizeMb}"
                              MinWidth="90" VerticalAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StringFormat={}{0} MB}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="{loc:Loc largefiles.cancel}" Style="{StaticResource SecondaryButton}"
                            Command="{Binding CancelCommand}"
                            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
                    <Button Content="{loc:Loc largefiles.analyze}" Style="{StaticResource SecondaryButton}"
                            Command="{Binding ScanCommand}" Margin="12,0,0,0" />
                    <Button Content="{loc:Loc largefiles.delete}" Style="{StaticResource PrimaryButton}"
                            Command="{Binding DeleteSelectedCommand}" Margin="12,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== Auswahl-Zusammenfassung (wie im Kategorien-Tab) ===== -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,14,0,0" Padding="22,18"
                ClipToBounds="True"
                Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Rectangle Grid.ColumnSpan="2" Margin="-22,-18" Fill="{DynamicResource HeroGlowBrush}"
                           IsHitTestVisible="False" />
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{loc:Loc cleaner.selectedLabel}" Style="{StaticResource MutedText}"
                               FontSize="11.5" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding SelectionSummary}" FontSize="19" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0,4,0,0" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="{loc:Loc common.selectAll}" Style="{StaticResource SecondaryButton}"
                            Padding="12,6" FontSize="12.5" Command="{Binding SelectAllCommand}" />
                    <Button Content="{loc:Loc common.deselectAll}" Style="{StaticResource SecondaryButton}"
                            Padding="12,6" FontSize="12.5" Margin="10,0,0,0"
                            Command="{Binding DeselectAllCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== Spaltenköpfe (klickbar = Sortierung) ===== -->
        <Grid Grid.Row="2" Margin="10,16,10,6"
              Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="110" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Width="30" />
            <Button Grid.Column="1" Content="{loc:Loc largefiles.column.name}"
                    Command="{Binding SortCommand}" CommandParameter="FileName"
                    HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0"
                    Padding="0" Cursor="Hand" FontSize="11.5" FontWeight="SemiBold"
                    Foreground="{DynamicResource TextMutedBrush}" />
            <Button Grid.Column="2" Content="{loc:Loc largefiles.column.size}"
                    Command="{Binding SortCommand}" CommandParameter="SizeBytes"
                    HorizontalContentAlignment="Right" Background="Transparent" BorderThickness="0"
                    Padding="0" Cursor="Hand" FontSize="11.5" FontWeight="SemiBold"
                    Foreground="{DynamicResource TextMutedBrush}" />
            <Button Grid.Column="3" Content="{loc:Loc largefiles.column.modified}"
                    Command="{Binding SortCommand}" CommandParameter="LastWriteTime"
                    HorizontalContentAlignment="Right" Background="Transparent" BorderThickness="0"
                    Padding="0" Cursor="Hand" FontSize="11.5" FontWeight="SemiBold"
                    Foreground="{DynamicResource TextMutedBrush}" />
        </Grid>

        <!-- ===== Leerhinweis ===== -->
        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Style="{StaticResource MutedText}"
                   HorizontalAlignment="Center" Margin="0,40,0,0" TextWrapping="Wrap"
                   Visibility="{Binding ShowEmptyHint, Converter={StaticResource BoolToVis}}" />

        <!-- ===== Dateiliste (virtualisiert wie die Kategorie-Vorschau) ===== -->
        <Border Grid.Row="3" Background="{DynamicResource BgDeepBrush}" CornerRadius="10"
                Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
            <ListBox ItemsSource="{Binding FilesView}" Background="Transparent" BorderThickness="0"
                     Padding="6" HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.CanContentScroll="True"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,1" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="row" Background="{TemplateBinding Background}"
                                            CornerRadius="6" Padding="10,7">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="row" Property="Background"
                                                    Value="{DynamicResource SurfaceHoverBrush}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid ToolTip="{Binding FullPath}">
                            <Grid.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick"
                                              Command="{Binding DataContext.OpenInExplorerCommand,
                                                        RelativeSource={RelativeSource AncestorType=ListBox}}"
                                              CommandParameter="{Binding}" />
                            </Grid.InputBindings>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="110" />
                                <ColumnDefinition Width="110" />
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      VerticalAlignment="Center" Margin="0,0,14,0" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding FileName}" FontSize="13" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding FolderPath}" Foreground="{DynamicResource TextMutedBrush}"
                                           FontSize="11" Margin="0,1,0,0" TextTrimming="CharacterEllipsis" />
                                <!-- Fehlgeschlagenes Verschieben: Zeile bleibt stehen und sagt warum -->
                                <TextBlock Text="{loc:Loc largefiles.deleteFailed}"
                                           Foreground="{DynamicResource DangerBrush}" FontSize="11"
                                           Margin="0,2,0,0"
                                           Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
                                <!-- Von PathSafety blockiert: kein Löschversuch, eigene Meldung statt "fehlgeschlagen" -->
                                <TextBlock Text="{loc:Loc largefiles.protected}"
                                           Foreground="{DynamicResource WarnBrush}" FontSize="11"
                                           Margin="0,2,0,0"
                                           Visibility="{Binding IsProtected, Converter={StaticResource BoolToVis}}" />
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="{Binding SizeDisplay}" VerticalAlignment="Center"
                                       FontSize="12.5" FontFamily="{StaticResource MonoFont}"
                                       TextAlignment="Right" />
                            <TextBlock Grid.Column="3" Text="{Binding DateDisplay}" VerticalAlignment="Center"
                                       FontSize="12.5" Foreground="{DynamicResource TextMutedBrush}"
                                       TextAlignment="Right" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
